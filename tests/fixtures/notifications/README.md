# Notification Contract Fixtures

These fixtures are structural examples for the draft notification-scheduling contract family.

They verify schema shape, required fields, closed enums, and representative schedule combinations. Example ids and repeated hexadecimal digests are placeholders. They are not computed preimage-and-digest conformance vectors unless a future manifest entry explicitly declares that scope.
